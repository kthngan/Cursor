# Get your API key from https://cursor.com/dashboard/integrations
CURSOR_API_KEY=cursor_your_key_here

# Password entered in the web app (use this on your iPhone login screen)
ACCESS_TOKEN=lewis-2026-test

# Listen on all interfaces so your iPhone on the same Wi-Fi can connect
HOST=0.0.0.0

# Repo root so Composer loads .cursor/skills/lewis-schedule-import/
WORKSPACE_DIR=

# Local port
PORT=8790

# Listen on all interfaces so iPhone on same Wi-Fi can connect (default 0.0.0.0)
HOST=0.0.0.0
